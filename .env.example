# Modal workspace name (for constructing endpoint URLs)
MODAL_WORKSPACE=

# Modal Proxy Auth tokens (create at https://modal.com → Settings → Proxy Auth Tokens)
MODAL_PROXY_TOKEN_ID=
MODAL_PROXY_TOKEN_SECRET=

# HuggingFace token (optional, only needed for gated models)
HF_TOKEN=

# Coder endpoint (set automatically by run.sh from MODAL_WORKSPACE)
OPENAI_BASE_URL=
OPENAI_API_KEY=EMPTY
OPENAI_MODEL=unsloth/Qwen3-Coder-Next-FP8-Dynamic

# VLM endpoint (set automatically by run.sh from MODAL_WORKSPACE)
VLM_BASE_URL=
VLM_MODEL=Qwen/Qwen3-VL-32B-Thinking-FP8
